<div class="dash-welcome">
  <p class="dash-welcome-eyebrow"><%= link_to "Instrutores", dashboard_instructors_path, class: "back-link" %> / Editar</p>
  <h1 class="dash-welcome-title"><%= @instructor.name %></h1>
</div>

<div class="analytics-card">
  <%= render "form", instructor: @instructor %>
</div>
