<div class="dash-welcome">
  <p class="dash-welcome-eyebrow"><%= link_to "Torneios", dashboard_tournaments_path, class: "back-link" %> / Editar</p>
  <h1 class="dash-welcome-title"><%= @tournament.name %></h1>
</div>

<div class="analytics-card">
  <%= render "form", tournament: @tournament %>
</div>
