<% content_for :title, "Editar — #{@product.name}" %>

<div class="dash-welcome">
  <p class="dash-welcome-eyebrow"><%= link_to "Vitrine de Produtos", dashboard_brands_products_path %></p>
  <h1 class="dash-welcome-title">Editar Produto</h1>
  <p class="dash-welcome-sub"><%= @product.name %></p>
</div>

<div class="prod-form-card">
  <%= render "form", product: @product %>
</div>
